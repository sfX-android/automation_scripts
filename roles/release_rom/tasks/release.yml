---

- name: "Release ROM for {{ BDEVICE }}"
  copy:
    src: "{{ item }}"
    dest: "{{ rempath }}"
  delegate_to: "{{ vault_RELEASESVR }}"
  loop:
    - ROMFILE.stdout
    - "{{ ROMFILE.stdout }}.sha256sum"
