---

- debug:
    var: semaphore_vars

- set_fact:
    PIPETWRPBUILDVER: "PREVIEW-{{ semaphore_vars.task_details['target_version'] }}"
    env_version: "{{ PIPETWRPBUILDVER }}"

- debug:
    msg: "SHRP version/buildid has been set to: {{ PIPETWRPBUILDVER }}"
