---

- name: "Ensure the defined sources path exists"
  file:
    path: "{{ SRCPATH }}"
    state: directory

- name: "Ensure no compat bin links exist"
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - "~/.local/bin/python"
    - "~/.local/bin/gcc"
