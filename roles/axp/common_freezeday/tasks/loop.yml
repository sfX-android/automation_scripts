---

- name: "Loop over freezeday_app_repos"
  include_tasks: build_apps.yml
  loop: "{{ freezeday_app_repos }}"
  loop_control:
    loop_var: lp
    pause: 300  # 5m delay between each run
  vars:
    repo: "{{ lp }}"

