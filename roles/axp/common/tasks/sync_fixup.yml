---

- name: "Apply Sync fixup (17.1)"
  ansible.builtin.shell: |
      {{ DOS_INIT }}
      cd {{ SRCPATH }}/vendor/lineage
      git revert e85694867b50377d7cfc82edd7417dde0b5ac386 04553ee1e0865dab7cd0df0368f99cb0497447e7 # stupid change, breaking repopick!
  when: android_version_int | int == 10

- name: "Apply Sync fixup (18.1)"
  ansible.builtin.shell: |
      {{ DOS_INIT }}
      cd {{ SRCPATH }}/vendor/lineage
      git revert 3bea2ad4b3bf583840226eff100d77ebf2b674b3 # stupid change, breaking repopick!
  when: android_version_int | int == 11
