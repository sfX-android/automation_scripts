---

- name: "Remove AudioFX app"
  ansible.builtin.shell: >
    awk -i inplace '!/AudioFX/' vendor/lineage/config/*.mk
  args:
    chdir: "{{ SRCPATH }}"

- name: "Apply vendor_axp patches"
  ansible.builtin.shell: |
      {{ DOS_INIT }}
      patch -r - --no-backup-if-mismatch --forward --ignore-whitespace --verbose -p1 < {{ item.patch }}
  args:
    executable: /bin/bash
    chdir: "{{ SRCPATH }}/{{ item.path }}"
  loop:
    - path: packages/apps/Settings
      patch: vendor_axp/patches/common/a10/0002_packages_apps_Settings_src_com_android_settings_accounts_AccountTypePreferenceLoader_java.patch
