---

- name: "Manage a service"
  hosts: buildserver
  gather_facts: true
      
  tasks:
    - name: "action: {{ servicename }} on {{ servicename }}"
      become: yes
      service:
        name: "{{ servicename }}"
        state: "{{ servicename_state }}"
